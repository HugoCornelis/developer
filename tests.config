#!/usr/bin/perl -w
#!/usr/bin/perl -d:ptkdb
#
#
#

use strict;


# $Format: "my $package_name = \"${package}\";"$
my $package_name = "installer";

# $Format: "my $package_version = \"${label}\";"$
my $package_version = "a599718d1a785a98621afb049aa70b206151590c-0";


my $config
    = {
       core_directory => './',
       description => 'Neurospaces developer package functional tests.',
       environment_settings => {
				NEUROSPACES_BUILD_TESTING => ('testing from ' . `pwd`),
			       },
       package => {
		   name => $package_name,
		   version => $package_version,
		  }
      };


return $config;


