#!/usr/bin/perl -w
#!/usr/bin/perl -d:ptkdb
#
#
#

use strict;


# $Format: "my $package_name = \"${package}\";"$
my $package_name = "installer";

# $Format: "my $package_version = \"${label}\";"$
my $package_version = "2f0240e31bcec6a5bae02c91d03bd8332110d212-0";


my $config
    = {
       core_directory => '../',
       description => 'Configure the tester when run from this directory',
       outputs_dir => './html',
       package => {
		   name => $package_name,
		   version => $package_version,
		  },
       tests_directory => './specifications',
      };


return $config;


